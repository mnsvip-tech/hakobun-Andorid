# Agents.md - Androidアプリ開発エージェント構成定義書

本ドキュメントは、AIエージェントが当プロジェクトで安全かつ効率的に動作するための役割、コンテキスト、および開発手順を定義します。

## 1. プロジェクト基本情報
* **プロジェクトタイプ**: Androidネイティブアプリ開発
* **推奨IDE**: Android Studio（Google公式IDE）
* **主要言語・フレームワーク**: Kotlin / Jetpack Compose (段階的移行推奨)
* **作業用ルートフォルダー**: `C:\Users\mnsvi\OneDrive\デスクトップ\Mobileアプリ\Android`

---

## 2. エージェントのセットアップ & 実行環境
AIエージェントは、Googleが提供するAI最適化済みの手順である **Android Skills** をインポートして動作します。

### セットアップ手順
1. **フォルダー接続**: エージェントを作業用ルートフォルダーにマウントします。
2. **Android Skills の読み込み**: エージェントのコンテキストに以下のコアSkillを学習させます。
   * **Migration Skill**: レガシーなXMLレイアウトからJetpack Composeへの安全な移行手順
   * **Performance Skill**: メモリリーク（LeakCanaryの活用）、プロファイラーを活用した描画最適化手順
   * **Policy Compliance Skill**: Google Playの最新規約チェックルール

### エージェントの基本命令 (System Prompt)
> "あなたはAndroid StudioとAndroid Skillsに精通したシニアAndroidエンジニアです。コードを生成・修正する際は、常に最新のAndroid APIガイドラインおよびGoogle Playのポリシーに準拠してください。"

---

## 3. 開発コア・ガイドライン (Android Skillsの適用)

### ① UI・レイアウト設計 (XML to Compose)
* 新規画面はすべて **Jetpack Compose** で構築します。
* 既存のXML画面を修正する場合は、Android Skillsに基づき、コンポーネント単位で段階的にComposeへリファクタリングを行ってください。
* 単にコードを書き換えるだけでなく、互換性（`AndroidView` や `ComposeView` の利用）とライフサイクルへの影響を考慮してください。

### ② パフォーマンス最適化
* **メモリ管理**: 非同期処理（Coroutines/Flow）における構造化された並行性を徹底し、メモリリークを防止します。
* **レンダリング**: Composeの不要な再コンポジション（Recomposition）を抑制するため、`Remember` や `Stable` アノテーションを適切に使用します。

---

## 4. Google Playポリシー準拠ガイドライン (必須)
アプリのリリースおよびアップデートにおいて、Google Playの規約を完全に遵守するために、エージェントは以下のチェックを自動実行してください。

| 遵守項目 | エージェントの実行アクション |
| :--- | :--- |
| **プライバシーとデータセキュリティ** | 個人情報やデバイスデータを取得・送信する場合、暗号化（HTTPS/TLS）を必須とし、データセーフティフォームの記述と矛盾がないか確認する。暗号化アルゴリズムには安全な暗号（AES-256等）を使用する。 |
| **パーミッションの最小化** | 実行時権限（Runtime Permissions）はアプリのコア機能に必要な最小限に留める。特にバックグラウンド位置情報やストレージへの全アクセス権は原則要求しない。 |
| **SDKの安全性** | 導入するすべての外部ライブラリ（SDK）がGoogle Playのポリシーに違反していないか（悪質なデータ収集や難読化されたコードの実行がないか）を確認する。 |
| **ターゲットAPIレベル** | `build.gradle` の `targetSdk` が、Google Playが要求する最新のAPIレベル（現状の要件に追従）を満たしているか確認する。 |

---

## 5. エージェント作業ワークフロー
エージェントがタスクを実行する際は、以下のステップを遵守してください。

1. **静的解析の実行**: コード修正前に Android Studio の `Lint` 機能を利用し、潜在的なバグやポリシー違反の兆候（非推奨APIの利用など）を検知する。
2. **コード生成・修正**: Android Skills に従い、セキュアでクリーンなコードを生成する。
3. **ポリシー・検証**: 変更したコードが「Google Playポリシー」および「パフォーマンス基準」を満たしているかをセルフチェックする。
4. **報告**: 修正内容とともに、影響のある範囲、変更した依存関係（SDK）、およびテスト結果を報告する。
